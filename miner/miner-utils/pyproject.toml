[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"

[project]
name = "miner-utils"
version = "0.1.0"
description = "Shared utilities for Pearl miner packages"
requires-python = ">=3.12"
dependencies = [
  "loguru>=0.7.0",
]
